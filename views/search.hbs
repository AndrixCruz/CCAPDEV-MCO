<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Reviews - Reviews</title>
    <link rel="stylesheet" href="design.css">
    <script src="search.js"></script>
    <style>
        .review-info {
            display: none;
            padding: 10px;
        }
        .establishment-card {
            display: flex;
            flex-direction: row;
            align-items: center;
            margin-bottom: 20px;
        }
        .establishment-card img {
            width: 100px;
            height: 100px;
            margin-right: 20px;
        }
    </style>
</head>
<body>
    {{#if authenticated}}
    {{> navbarLogged}}
    {{else}}
    {{> navbar}}
    {{/if}}

    <section class="hero">
        <div class="container">
            <h2>Welcome to Restaurant Reviews</h2>
            <p>Discover the best restaurants in your area and share your dining experiences with others.</p>
            <textarea id="search" name="search" rows="1" cols="50" placeholder="Search for restaurants..."></textarea>
    </section>

    <section class="featured-restaurants">
        <div class="container">
            <div id="title"><h2>List of Comments</h2></div>

            {{#each commentsList}}
            <div class="restaurant-card" style="justify-content: center;">
                <div style="display: flex; flex-direction: column; align-items: center;">
                    <h3 style="text-align: center;">{{this.commentText}}</h3>
                    <h3 style="text-align: center;">Restaurant: {{this.company}}</h3>
                    <h3 style="text-align: center;">Rating: {{this.rating}} / 5</h3>
                </div>
            </div>
            {{/each}}

            <div id="title"><h2>List of Restaurants</h2></div>

            {{#each restaurantsList}}
            <div class="restaurant-card" style="justify-content;">
                <div style="display: flex; flex-direction: column; align-items: center;">
                    <h3 style="text-align: center;">{{this.name}}</h3>
                </div>
            </div>
            {{/each}}
        </div>
    </section>
</body>
</html>